"""Processing pipeline modules for FonixFlow Splat.

COLMAP/GLOMAP SfM, dense MVS, Gaussian-splat trainers (Brush via app.py, gsplat
MCMC), the sparse PLY fallback, the 3D alignment preview, and camera export.
`app.py` imports from here; these modules do not import each other.
"""
